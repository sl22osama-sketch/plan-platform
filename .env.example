# Neon / Postgres connection string (paste the FULL URL into Vercel)
# Example: postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Login for the (single) coordinator account
COORDINATOR_EMAIL=coordinator@example.com
COORDINATOR_PASSWORD=change-me-please

# Used to sign the session cookie (any long random string)
AUTH_SECRET=replace-with-a-long-random-string
